import React, { useState } from 'react';
import Header from '../../components/header';

const Dependentes = () => {
    return (
        <>
            <Header />
            <h1>Tela dos Dependentes</h1>
        </>
    );
};

export default Dependentes;
